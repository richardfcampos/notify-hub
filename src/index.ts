/**
 * Placeholder module root. Real entrypoints live in src/bin/{api,worker}.ts
 * (added in later phases). This file exists so the initial project scaffold
 * has something valid for tsc to compile.
 */
export const NOTIFY_HUB_VERSION = '0.1.0'
